# HTTP client

The HTTP layer makes raw socket-level GET requests with no third-party HTTP
library. TLS, chunked transfer-encoding, redirect following, and response
caching are all handled here.

## HTTPClient

::: go2web.http.client.HTTPClient

## Response

::: go2web.http.client.Response

## HTTPError

::: go2web.http.client.HTTPError
